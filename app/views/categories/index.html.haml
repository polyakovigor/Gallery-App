.page-header
  %h1 Categories
.row
  .col-sm-1
    = link_to new_category_path, data: { toggle: 'tooltip' }, title: 'Add category' do
      %span.glyphicon.glyphicon-folder-open
.row
  - @categories.each do |category|
    .col-md-2.col-sm-4.col-xs-6
      = link_to category do
        = image_tag category.images.any? ? category.images.first.picture : 'empty-folder.png', size: '150', data: { toggle: 'tooltip' }, title: category.name, class: 'img-rounded'