<%= form_for(image, method: :put) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit 'Rename' %>
  </div>
<% end %>