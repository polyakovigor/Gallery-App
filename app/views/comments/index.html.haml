.page-header
  %h1 Comments
- if @comments.present?
  = paginate @comments
%ul
  - @comments.to_a.each do |comment|
    %li
      %p
        %span.text-muted
          = comment.user.full_name
          at
          = local_datetime(comment.created_at)
        %br
        = comment.body