.card
  .card-block
    .row
      .col-sm-11
        %p.card-text
          %span.text-muted
            = link_to comment.user.full_name, '#'
            commented #{ time_ago_in_words(comment.created_at) } ago.
          %br
          = comment.body