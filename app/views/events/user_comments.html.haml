.page-header
  %h1.text-info
    #{@user.email}
    %small comments:
- if @user.comments.present?
  %table.table
    %thead
      %tr
        %th Image
        %th Comment
        %th Time
    %tbody
      - @user.comments.each do |comment|
        %tr
          %td= link_to comment.image.title, image_path(comment.image)
          %td.card-text
            = comment.body
          %td= local_datetime(comment.created_at)
- else
  .panel.panel-body NO INFO
