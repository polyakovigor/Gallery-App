.page-header
  %h1 Events(User Comments)
%table.table
  %thead
    %tr
      %th Image
      %th Comment
      %th Time
  %tbody
    - @user.comments.to_a.each do |comment|
      %tr
        %td= link_to comment.image.title, image_path(comment.image)
        %td= comment.body
        %td= local_datetime(comment.created_at)