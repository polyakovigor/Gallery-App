.page-header
  %h1 Events(User Likes)
%table.table
  %thead
    %tr
      %th Image
      %th Time
  %tbody
    - @user.likes.each do |like|
      %tr
        %td= link_to like.image.title, image_path(like.image)
        %td= like.created_at.strftime('%H:%M:%S %d %B %Y')