.page-header
  %h1 Events(#{@user.email} Navigation)
%table.table
  %thead
    %tr
      %th Url
      %th Action
      %th Time
  %tbody
    - @events.each do |event|
      %tr
        %td= event.url
        %td= event.action
        %td= local_datetime(event.created_at)