.page-header
  %h1 Events(#{@user.email} Navigation)
%table.table
  %thead
    %tr
      %th Url
      %th Time
  %tbody
    - @events.each do |event|
      %tr
        %td= link_to event.url
        %td= local_datetime(event.created_at)