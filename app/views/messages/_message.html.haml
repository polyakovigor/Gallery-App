.card
  .card-block
    .row
      .col-sm-11
        %p.card-text
          %span.text-muted
            = message.user.full_name
            at
            = local_datetime(message.created_at)
            says:
          %br
          = message.body
