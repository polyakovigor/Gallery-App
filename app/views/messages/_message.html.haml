.card
  .card-block
    .row
      .col-sm-1
        = message.user
      .col-sm-11
        %p.card-text
          %span.text-muted
            = message.user.first_name
            at
            = message.timestamp
          %br
          = message.body
%hr